/** Row shape for `public.product_master` (Product Configuration admin). */
export type ProductMasterRow = {
  id: string
  product_description: string
  system_type: string | null
  product_type: string | null
  is_active: boolean
  created_at: string
  updated_at: string
}
